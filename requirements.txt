# PigeonPhoto_Automator - Requirements
# Python 3.9-3.11 (3.13 may have compatibility issues with some packages)

# Core Computer Vision
# Use opencv-python-headless for server deployments (no GUI dependencies)
# IMPORTANT: Do NOT install opencv-python (regular version) - it requires GUI libraries
# If you see libGL.so.1 errors, ensure opencv-python is NOT installed
opencv-python-headless>=4.8.0,<5.0.0
numpy>=1.24.0,<2.0.0
scikit-image>=0.21.0

# Segmentation
rembg>=2.0.50
onnxruntime>=1.16.0

# Pose Estimation
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Scientific Computing
scipy>=1.11.0

# Image Processing
Pillow>=10.0.0

# Web Framework (MVP)
streamlit>=1.28.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0

